extern char curr_dir[300];
extern char home_dir[300];
void ls_path(char* file,int a_flag);
void ls(char** commands,int cmd_num);
void ls_l(char* file,int a_flag);
void ls_l_print(char* file,int a_flag);